# راه‌اندازی هشدار خطا در تلگرام

## وضعیت فعلی

کد ارسال هشدار آماده است، اما تا زمانی که Bot Token و Chat ID در Supabase Secrets قرار نگیرند، هیچ پیامی ارسال نمی‌شود.

فقط خطاهای مهم زیر هشدار ایجاد می‌کنند:

- Fatal
- Payment
- Registration
- Storage

پیام تلگرام شامل شماره تماس، کارت، شبا، ایمیل، Token، متن کامل خطا یا اطلاعات کودک نیست. جزئیات فقط در پنل امن مدیریت دیده می‌شود.

## کاری که مالک باید انجام دهد

1. در تلگرام وارد `@BotFather` شوید.
2. دستور `/newbot` را بزنید.
3. نام و Username ربات را انتخاب کنید.
4. Bot Token را امن تحویل دهید.
5. ربات ساخته‌شده را باز کنید و یک پیام مانند «شروع» برایش بفرستید.
6. اگر پیام باید وارد گروه شود، ربات را به گروه اضافه کنید و یک پیام در گروه بفرستید.

بعد از ارسال پیام اولیه، Chat ID استخراج و این Secretها جداگانه برای هر پروژه تنظیم می‌شوند:

- `TELEGRAM_BOT_TOKEN`
- `TELEGRAM_CHAT_ID`
- `TELEGRAM_PROJECT_NAME`

پس از تنظیم، یک پیام آزمایشی بدون اطلاعات شخصی ارسال می‌شود.
