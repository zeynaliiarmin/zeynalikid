import type {AssistantKnowledge} from '../utils/assistantMatch';
export interface AssistantSettings {enabled:boolean;welcome_message:string;fallback_message:string;disclaimer:string;updated_at?:string;}
export interface AssistantPublicData {knowledge:AssistantKnowledge[];settings:AssistantSettings;}
const base=String(import.meta.env.VITE_SUPABASE_URL||'').replace(/\/$/,'');const url=`${base}/functions/v1/assistant-public`;const CACHE_KEY='zk_assistant_public_v1';let memory:{at:number;value:AssistantPublicData}|null=null;
export async function fetchAssistantData():Promise<AssistantPublicData>{const now=Date.now();if(memory&&now-memory.at<300000)return memory.value;try{const cached=JSON.parse(sessionStorage.getItem(CACHE_KEY)||'null');if(cached?.value&&now-Number(cached.at||0)<300000){memory=cached;return cached.value}}catch{}const response=await fetch(url,{headers:{'Content-Type':'application/json'}});if(!response.ok)throw new Error('دستیار در دسترس نیست');const body=await response.json();const value={knowledge:Array.isArray(body.knowledge)?body.knowledge:[],settings:{enabled:body.settings?.enabled!==false,welcome_message:String(body.settings?.welcome_message||'سلام! سؤال خود را بپرسید.'),fallback_message:String(body.settings?.fallback_message||'پاسخ تأییدشده‌ای پیدا نکردم.'),disclaimer:String(body.settings?.disclaimer||'این دستیار جایگزین مشاوره تخصصی نیست.'),updated_at:body.settings?.updated_at}};memory={at:now,value};try{sessionStorage.setItem(CACHE_KEY,JSON.stringify(memory))}catch{}return value}
async function post(payload:Record<string,unknown>){const response=await fetch(url,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});if(!response.ok)throw new Error('ثبت انجام نشد');return response.json()}
export const submitAssistantUnanswered=(question:string,page_path:string)=>post({action:'unanswered',question,page_path});
export const submitAssistantFeedback=(knowledge_id:string,helpful:boolean)=>post({action:'feedback',knowledge_id,helpful});
