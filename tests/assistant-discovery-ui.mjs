import assert from 'node:assert/strict';import puppeteer from 'puppeteer';
const base=process.env.TEST_BASE_URL||'http://127.0.0.1:4173',browser=await puppeteer.launch({headless:true,executablePath:process.env.PUPPETEER_EXECUTABLE_PATH||undefined,args:['--no-sandbox','--disable-dev-shm-usage']});
try{const page=await browser.newPage();await page.setViewport({width:390,height:844});await page.setRequestInterception(true);page.on('request',r=>{if(r.url().includes('/functions/v1/assistant-public')){if(r.method()==='OPTIONS')return r.respond({status:204,headers:{'access-control-allow-origin':'*','access-control-allow-headers':'*','access-control-allow-methods':'GET,POST,OPTIONS'},body:''});return r.respond({status:200,contentType:'application/json',headers:{'access-control-allow-origin':'*'},body:JSON.stringify({settings:{enabled:true,welcome_message:'سلام',fallback_message:'',disclaimer:'',suggested_questions:[]},knowledge:[]})});}r.continue()});await page.goto(base+'/',{waitUntil:'domcontentloaded'});await page.waitForFunction(()=>document.body.innerText.includes('همراه شما هستیم'),{timeout:5000});const shape=await page.$eval('.zka-discovery',n=>({width:n.getBoundingClientRect().width,height:n.getBoundingClientRect().height,svg:!!n.querySelector('svg'),fill:getComputedStyle(n.querySelector('path')).fill}));assert.ok(shape.width>shape.height*3);assert.equal(shape.svg,true);await page.evaluate(()=>window.scrollTo(0,120));await page.waitForFunction(()=>!document.querySelector('.zka-discovery'),{timeout:3000});await page.evaluate(()=>{for(const key of Object.keys(sessionStorage))if(key.includes('assistant_discovery_'))sessionStorage.removeItem(key);for(const key of ['zeynalikid_assistant_discovery_initial','farzandman_assistant_discovery_initial'])localStorage.setItem(key,'1');sessionStorage.setItem('zeynalikid_assistant_discovery_elapsed','299500');sessionStorage.setItem('farzandman_assistant_discovery_elapsed','299500')});await page.reload({waitUntil:'domcontentloaded'});await page.waitForFunction(()=>document.body.innerText.includes('راهنمایی لازم دارید؟'),{timeout:4000});await page.mouse.click(380,800);await page.waitForFunction(()=>!document.querySelector('.zka-discovery'),{timeout:3000});await page.goto(base+'/consultation',{waitUntil:'domcontentloaded'});await new Promise(r=>setTimeout(r,1500));assert.equal(await page.$('.zka-discovery'),null,'discovery hint must not run during consultation flow');console.log('Assistant discovery hint timing, dismissal and form exclusion passed.')}finally{await browser.close()}
