import {readFile} from 'node:fs/promises';
const read=path=>readFile(new URL(`../${path}`,import.meta.url),'utf8');const [migration,publicFn,adminFn,telegramFn,widget,app,adminPanel]=await Promise.all([read('supabase/migrations/20260825170000_free_knowledge_assistant.sql'),read('supabase/functions/assistant-public/index.ts'),read('supabase/functions/assistant-admin/index.ts'),read('supabase/functions/assistant-telegram/index.ts'),read('src/components/AssistantWidget.tsx'),read('src/App.tsx'),read('src/admin/AdminPanel.tsx')]);
const failures=[];const need=(source,text,label)=>{if(!source.includes(text))failures.push(label)};
need(migration,'assistant_knowledge enable row level security','knowledge RLS missing');need(migration,'assistant_browser_deny','explicit browser deny policy missing');need(migration,'assistant_bot_states','Telegram conversation state table missing');
need(publicFn,".eq('status','published')",'public endpoint may expose drafts');need(publicFn,"action==='unanswered'",'unanswered workflow missing');need(publicFn,"action==='feedback'",'feedback workflow missing');
need(adminFn,'validateAdminSession','admin assistant endpoint lacks session validation');need(adminFn,"action==='batch_import'",'admin import missing');need(adminFn,'admin_audit_logs','assistant admin audit missing');
need(telegramFn,"X-Telegram-Bot-Api-Secret-Token",'Telegram webhook signature validation missing');need(telegramFn,"chatId!==owner()",'Telegram owner-only validation missing');need(telegramFn,"status:'draft'",'Telegram additions must start as draft');need(telegramFn,'/delete ID CONFIRM','Telegram destructive confirmation missing');
need(widget,'matchAssistantKnowledge','browser-local matching missing');need(widget,'ارسال ناشناس برای بررسی','explicit unanswered consent missing');need(widget,'شماره تماس یا اطلاعات پزشکی خصوصی وارد نکنید','privacy warning missing');need(app,'<AssistantWidget','assistant widget not mounted');need(adminPanel,"id:'assistant'",'assistant admin tab missing');
const combined=[publicFn,adminFn,telegramFn,widget].join('\n');if(/api\.(openai|deepseek)\.com|generativelanguage\.googleapis\.com/i.test(combined))failures.push('paid external AI API found in free assistant');if(/ASSISTANT_TELEGRAM_BOT_TOKEN\s*=\s*['"][^'"]+/i.test(combined))failures.push('assistant bot token hard-coded');if(/service_role/i.test(widget))failures.push('service role leaked into assistant widget');
if(failures.length){console.error(failures.join('\n'));process.exit(1)}console.log('Free knowledge assistant security contracts passed.');
