# پایلوت دستیار دانش و پیش‌نمایش مولد زینالیکید

## وضعیت دستیار عمومی

دستیار عمومی سایت هنوز مدل زبانی مولد را فراخوانی نمی‌کند. پاسخ را فقط از دانش تأییدشده مدیر پیدا می‌کند و تطبیق داخل مرورگر انجام می‌شود.

- تطبیق فارسی و تحمل محدود غلط تایپی
- سؤال نامرتبط = بدون ساخت پاسخ
- عدم ارسال سؤال عمومی به سرویس هوش مصنوعی خارجی
- ارسال سؤال بدون پاسخ فقط با دکمه و رضایت صریح کاربر
- هشدار ثابت: جایگزین پزشک یا مشاور تخصصی نیست

## پیش‌نمایش مولد فقط برای مدیر

در تب **دستیار** پنل مدیریت، مدیر می‌تواند کیفیت پاسخ مولد را با Mistral آزمایش کند. این قابلیت هنوز در دستیار عمومی نمایش داده نمی‌شود.

روند پیش‌نمایش:

1. نشست مدیریت در Edge Function اعتبارسنجی می‌شود.
2. سؤال آزمایشی از شماره تماس، ایمیل، کارت، شبا و شناسه‌های عددی حساس پاک‌سازی می‌شود.
3. فقط دانش `published` و `active` بازیابی می‌شود.
4. حداکثر شش مورد مرتبط انتخاب می‌شود.
5. سؤال آزمایشی و همان دانش مرتبط به `mistral-small-latest` ارسال می‌شود.
6. پاسخ و فهرست دانش استفاده‌شده در پنل نشان داده می‌شود.

قواعد:

- سؤال و پاسخ آزمایشی در جدول جدیدی ذخیره نمی‌شوند.
- در Audit فقط موفقیت، مدل و تعداد منابع ثبت می‌شود؛ متن سؤال ثبت نمی‌شود.
- اگر دانش مرتبط پیدا نشود، Mistral فراخوانی نمی‌شود.
- مدل موظف است فقط براساس دانش تأییدشده پاسخ دهد و اطلاعات یا توصیه پزشکی جدید نسازد.
- محدودیت پیش‌نمایش: حداکثر ۵ درخواست در دقیقه و ۲۰ درخواست در روز برای نشست مدیر.
- API Key فقط در Supabase Edge Secrets قرار دارد و به مرورگر ارسال نمی‌شود.

## پنل مدیریت دانش

تب **دستیار** امکانات زیر را دارد:

- افزودن و ویرایش سؤال/پاسخ
- عبارت‌های مشابه و کلمات کلیدی
- دسته‌بندی، لینک مرتبط و منبع
- اولویت
- پیش‌نویس یا انتشار
- Import از FAQ، دوره‌ها و آموزش‌ها به‌صورت پیش‌نویس
- تطبیق داخلی سؤال قبل از انتشار
- پیش‌نمایش مولد فقط برای مدیر
- نمایش دانش استفاده‌شده در پاسخ مولد
- سؤال‌های بدون پاسخ و تعداد تکرار
- آمار بازخورد مفید/غیرمفید
- تنظیم پیام خوش‌آمد، پاسخ جایگزین و Disclaimer

## ربات مدیریت دانش

ربات: `@zeynalikid_knowledge_admin_bot`

فقط Chat ID مالک و Header محرمانه Webhook پذیرفته می‌شود. افراد دیگر حتی با Start هیچ دسترسی یا پاسخی دریافت نمی‌کنند.

دستورات:

```text
/add
/list
/test سؤال
/unanswered
/publish ID
/draft ID
/delete ID CONFIRM
/cancel
/help
```

### افزودن مرحله‌ای

1. `/add`
2. نوشتن سؤال
3. نوشتن پاسخ تأییدشده
4. کلمات کلیدی یا `-`
5. دسته‌بندی یا `-`
6. لینک مرتبط یا `-`
7. مورد به‌صورت پیش‌نویس ذخیره می‌شود
8. انتشار با `/publish ID`

## امنیت و حریم خصوصی

- Service Role فقط در Edge Functions است.
- جداول دستیار RLS و Policy صریح منع مرورگر دارند.
- Public endpoint فقط موارد منتشرشده را برمی‌گرداند.
- مدیریت پنل و پیش‌نمایش Mistral فقط با نشست معتبر ادمین انجام می‌شود.
- Bot Token، Chat ID، Webhook Secret و Mistral API Key فقط در Supabase Edge Secrets هستند.
- شماره تماس، کارت و ایمیل از سؤال‌های بدون پاسخ حذف می‌شوند.
- متن کامل سؤال‌های موفق کاربران عمومی به سرور ارسال نمی‌شود؛ تطبیق عمومی داخل مرورگر انجام می‌شود.

## لغو پایلوت عمومی

از تنظیمات تب دستیار، گزینه «نمایش دستیار در سایت» خاموش می‌شود. داده‌های دانش حذف نمی‌شوند و بعداً قابل فعال‌سازی هستند. خاموش‌کردن دستیار عمومی مستقل از پیش‌نمایش مدیریتی است.
