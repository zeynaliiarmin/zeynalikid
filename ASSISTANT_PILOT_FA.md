# دستیار مولد زینالیکید

## وضعیت

دستیار مولد روی سایت عمومی و پنل مدیریت فعال است. کلید Mistral فقط در Supabase Edge Secrets قرار دارد و مرورگر مستقیماً با Provider ارتباط ندارد.

## دستیار عمومی

- پاسخ مولد براساس دانش عمومی منتشرشده
- دکمه انتقال فقط به مسیرهای عمومی تأییدشده
- پیشنهاد سؤال بعدی بعد از هر پاسخ
- پاسخ دوستانه و پیشنهادهای پیش‌فرض برای سؤال نامرتبط
- ردکردن سؤال‌های مدیریتی پیش از فراخوانی مدل
- عدم دسترسی به جدول دانش مدیریتی
- حداکثر ۱۰ سؤال در ۳ دقیقه و ۱۰ سؤال در روز برای هر مرورگر
- بدون بازخورد «مفید بود؟ بله/خیر»
- بدون پنجره رضایت در رابط دستیار
- هشدار عدم ورود شماره تماس و اطلاعات پزشکی خصوصی

## دستیار پنل مدیریت

دکمه کوچک راهنمای هدفون‌دار در تمام تب‌های پنل نمایش داده می‌شود.

- فقط با نشست معتبر مدیر
- پاسخ براساس جدول مستقل `assistant_admin_knowledge`
- حداکثر ۱۰ سؤال در هر دو دقیقه
- دکمه انتقال به تب و بخش مرتبط
- پارامترهای `tab` و `focus` برای Deep Link
- تمرکز و برجسته‌سازی بخش مقصد
- عدم ثبت متن سؤال در Audit؛ فقط مدل، تعداد منبع و وضعیت فراخوانی ثبت می‌شود

## جداسازی دانش

- `assistant_knowledge`: فقط دانش عمومی سایت
- `assistant_admin_knowledge`: فقط راهنمای پنل مدیریت
- هر دو جدول RLS و Policy صریح منع `anon` و `authenticated` دارند.
- Endpoint عمومی هیچ Query به جدول مدیریتی ندارد.
- URLهای عمومی `/admin` یا مقصد خارج از فهرست مسیرهای مجاز را به دکمه تبدیل نمی‌کنند.

## مدیریت دانش از پنل

تب «دستیار» دو بخش دارد:

1. دانش عمومی سایت
2. راهنمای پنل مدیریت

دانش جدید ابتدا می‌تواند Draft باشد و پس از بررسی Published شود. برای دانش عمومی مسیر و عنوان دکمه ثبت می‌شود. برای دانش مدیریتی تب مقصد، متن بخش دقیق و عنوان دکمه انتقال ثبت می‌شود.

## آموزش با ربات تلگرام زینالیکید

ربات `@zeynalikid_knowledge_admin_bot` فقط در گفت‌وگوی خصوصی مالک فعال است.

```text
/add
/list
/test سؤال
/test_admin سؤال
/unanswered
/publish ID
/draft ID
/delete ID CONFIRM
/cancel
/help
```

پس از `/add`، ربات ابتدا نوع دانش «عمومی» یا «مدیریتی» را می‌پرسد. همه موارد جدید به‌صورت پیش‌نویس ذخیره می‌شوند و با `/publish ID` انتشار می‌یابند.

## داده و حریم خصوصی

برای پاسخ عمومی، سؤال پاک‌سازی‌شده و حداکثر شش بخش مرتبط از دانش عمومی تأییدشده به Mistral ارسال می‌شود. برای پاسخ مدیریتی فقط سؤال مدیر و دانش مدیریتی مرتبط ارسال می‌شود. رمز، API Key، Token، اطلاعات کاربران و Submissionها وارد Knowledge Base نمی‌شوند.
