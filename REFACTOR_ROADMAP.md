# نقشه راه Refactor تدریجی App.tsx و Design Tokens

هدف: کوچک کردن `src/App.tsx` (حدود ۷۰۰ خط) بدون هیچ ریسک شکستن فرم، فوکوس، اسکرول یا state موبایل.
روش: هر مرحله **فقط یک جابجایی کوچک**، بعد از هر مرحله تست و live، و در هر نقطه می‌توان دستور توقف داد.

---

## مرحله ۱ — استخراج Design Tokens (مورد ۲۳، بدون تغییر بصری)
**زمان تخمینی:** ۱۵ دقیقه  
**ریسک:** بسیار کم  
**چه کاری می‌کند:**
- فایل جدید `src/theme/tokens.ts`: ثابت‌های رنگ/سایه/اندازه را از فایل‌های `defaultSettings.ts` خارج نمی‌کند؛ فقط یک «لایه CSS variables» اضافه می‌کند که در `:root` و `[data-theme]` ست می‌شود.
- به‌جای استایل inline مثل `{boxShadow:'0 8px 18px ...'}`، از `var(--zk-btn-shadow)` استفاده می‌شود.
- `S` مثل قبل می‌ماند و فقط مقدارها به var تغییر می‌کند → UI دقیقاً مثل قبل.

**برگشت‌پذیری:** revert یک فایل + ۱۰ خط در App.tsx.

---

## مرحله ۲ — استخراج ثابت‌ها و توابع کمکی
**زمان تخمینی:** ۱۰ دقیقه  
**ریسک:** بسیار کم
- استخراج ثابت‌های `COURSE_TIMER_MS`, `emptyFd`, `emptyCourse`, `p2e`, `phonePlaceholder`, و توابع `validPhone`, `fullPhone`, `deliveryText`, `chooseDest` به `src/lib/courseFlow.ts`.
- App.tsx فقط useMemo/useState ها و JSX را نگه می‌دارد.

---

## مرحله ۳ — استخراج JSX بخش‌های مستقل به کامپوننت
**زمان تخمینی:** ۲۰ دقیقه  
**ریسک:** کم (باید props از AppContext بخوانند)
- هر بخش از JSX که از قبل شروع و پایان مشخص دارد به یک کامپوننت در `src/sections/` منتقل می‌شود:
  - `HomePage` (قبلاً هست)
  - `CoursesPage` (قبلاً هست)
  - `CourseShipModal`, `ReferralConsultBanner`, `CourseTimerBar`
  - `Footer`, `Header` (قبلاً هست)
- همه از `useAppContext()` استفاده می‌کنند، پس هیچ prop drilling ندارد.

---

## مرحله ۴ — کوچک کردن context value
**زمان تخمینی:** ۱۵ دقیقه  
**ریسک:** متوسط
- به‌جای پاس دادن ده‌ها تابع و مقدار، context به سه زیر-context تقسیم می‌شود: `AppUIContext` (T, S, css)، `AppFlowContext` (fd, course, courseResult)، `AppAdminContext` (adminAuthed, adminTab).
- هر زیر-context فقط در جایی که لازم است مصرف می‌شود → re-render کمتر.

---

## مرحله ۵ — حذف استایل‌های inline تکراری به کامپوننت‌های اتمی
**زمان تخمینی:** ۲۵ دقیقه  
**ریسک:** متوسط (باید بصری چک شود)
- کامپوننت‌های `<PrimaryButton>`, `<GhostButton>`, `<Chip>`, `<Field>`, `<Section>` ساخته شود.
- استایل‌های طولانی `style={{...S.btn, ...}}` به `variant` و `size` تبدیل شود.
- در هر جایگزینی، یک به یک و با تأیید بصری انجام می‌شود.

---

## ترتیب پیشنهادی اجرا
1. مرحله ۱ (توکن‌ها) — امن‌ترین، فوراً نگهداری را راحت‌تر می‌کند.
2. مرحله ۲ (توابع) — ریسک صفر.
3. استراحت، تست کامل.
4. مرحله ۳ (کامپوننت‌های JSX) — هر بخش را جدا commit می‌کنیم.
5. استراحت، تست کامل.
6. مرحله ۴ (context split) — بزرگترین تغییر، بعد از اینکه ۱-۳ stable بود.
7. مرحله ۵ (اتمیک کامپوننت‌ها) — آرایش ظاهری است، آخرش.

---

## تضمین‌های ایمنی (هندآف مانند قبل)
- هیچ‌گاه در یک مرحله هم استایل هم logic تغییر نمی‌کند.
- هیچ‌گاه state فرم / focus / scroll / کیبورد موبایل reset نخواهد شد.
- `defaultValue + onBlur` پایدار می‌ماند.
- `-webkit-` پرفیکس انیمیشن‌ها حفظ می‌شود.
- `referralReady` در App حفظ می‌شود.
- `sanitizeConsultants` برای banks/bank/wallet حفظ می‌شود.
- گارد auth همان‌طور که باید (instant Navigate بدون فلش؛ `/form` بدون گارد) باقی می‌ماند.

اگر تأیید کردید، با «شروع مرحله ۱» من یک مرحله را انجام می‌دهم، commit می‌کنم، و به شما نشان می‌دهم تا تأیید بصری بگیرم.
